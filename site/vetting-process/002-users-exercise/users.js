/**
 * @function sortBy
 * @desc Sorts a users array by the provided prop name and returns a NEW array
 * @param {Array} users
 * @param {String} prop
 * */
function sortBy(users, prop) {
  const newUsersArray = [...users];
  // sort here

  return newUsersArray;
}



// module.exports = sortBy;


